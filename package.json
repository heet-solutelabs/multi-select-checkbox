{
  "name": "multi-select-checkbox",
  "version": "3.1.2",
  "license": "MIT",
  "description": "Simple and lightweight multiple selection component with checkboxes, search and select-all",
  "author": {
    "name": "heet_gosaliya",
    "email": "heet.g@solutelabs.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/heet-solutelabs/multi-select-checkbox"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      [
        "@babel/preset-react",
        {
          "runtime": "automatic"
        }
      ]
    ]
  },
  "scripts": {
    "publish:npm": "rm -rf dist && mkdir dist &&  babel src -d dist --copy-files",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "devDependencies": {
    "@babel/preset-env": "^7.10.4",
    "@babel/core": "^7.11.6",
    "@babel/preset-react": "^7.13.13",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.3"
  },
  "keywords": [
    "react",
    "multi-select-checkbox"
  ],
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
